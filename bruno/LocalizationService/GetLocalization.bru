meta {
  name: GetLocalization
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/parlance.v1.LocalizationService/GetLocalization
  body: json
  auth: inherit
}

body:json {
  {
    "id": "{{localizationId}}"
  }
}

docs {
  # GetLocalization

  Retrieves a specific localization by ID.

  ## Request
  - `id` (required): UUID of the localization

  ## Response
  - `localization`: The localization object with translation, entry, and locale info

  ## Error Codes
  - `NOT_FOUND`: Localization with the given ID does not exist

  ## Example Use Case
  Fetch a specific translation to edit or display details.
}
